
벡터
방향 , 크기를 가진 물리량
힘,속도

좌표도 벡터로 표현이 가능
(30,30)  -> 0,0에서부터 나오는

벡터의 길이(스칼라)
벡터의 크기(피타고라스),물리량(원점에서 멀어진 크기)

단위벡터
크기가 1인 벡터 (방향벡터)

벡터의 스칼라곱
v x k  (x*k,y*k) 크기가 k배 커진다.

벡터의 정규화
본인  X 1 /벡터크기 

*벡터의 합연산*
오브젝트의 이동
v1 + v2 -> (v1.x+v2.x, v1.y+v2.y)
v1->cc
v2->이동값

프레임당 이동값
cc1.pos += 이동값


초당 이동값
 좌표            속도
             방향 * 스칼라
cc1.pos += 단위벡터 *초당이동값 * 델타


방향을 가지는 단위벡터를 구할때
원의반지름(벡터의크기)1로두면
(c*cos , c*sin)
(cos,sin) -> 단위벡터


*벡터의 -합연산*
두 벡터간의 차이 벡터를 만들 때


벡터의 곱연산

내적
스칼라
x1*x2 + y1*y2 -> |v1|*|v2|*cosA

벡터에서 한 축을 기준으로 투영된 길이를 구할 때
단위벡터를 이용하면 투영된 길이 값이 나옴

외적
벡터