
1. 벡터
방향과 크기를 가진 물리량
힘, 속도


2. 단위벡터 -> 방향벡터
길이가 1인 벡터


3. 벡터의 정규화 -> 방향만 남게 할 때
본인 * 1 / 본인의 크기


4. 벡터의 길이 -> 크기(스칼라)
v * k (x * k, y * k)


5. 벡터의 합연산
오브젝트의 이동
v1 + v2 -> (v1.x + v2.x, v1.y + v2.y)
v1 -> rc
v2 -> 이동값

프레임당 이동값
rc.pos += 이동값

초당 이동값
// 너무너무 중요

			 속도(veclocity)
			방향		   크기
rc.pos += 단위벡터 * 초당이동값 * delta

// 출발지가 같아야 같은 차원에 있는 벡터다.


방향을 가지는 단위벡터를 구할 때
원의 반지름을(벡터의 크기 -> 스칼라) 1로 두면
(c * cos, c * sin)
(cos, sin)



6. 벡터의 곱연산

내적
벡터와 벡터를 곱하면 스칼라가 나옴

외적
벡터와 벡터를 곱하면 벡터가 나옴

